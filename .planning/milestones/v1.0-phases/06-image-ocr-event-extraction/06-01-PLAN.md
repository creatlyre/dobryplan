---
phase: 06-image-ocr-event-extraction
plan: 01
type: execute
wave: 1
depends_on: [05-05]
files_modified:
  - app/events/ocr.py
  - app/events/routes.py
  - app/templates/calendar.html
  - app/templates/partials/quick_add_modal.html
  - tests/test_events_api.py
  - tests/test_calendar_views.py
  - requirements.txt
autonomous: true
requirements: [OCR-01, OCR-02, OCR-03]
---

<objective>
Implement Phase 6 OCR extraction in the existing Quick Add flow: image upload, OCR+NLP extraction, confidence-aware review, editable fields, and fallback with raw text.
</objective>

<context>
@.planning/phases/06-image-ocr-event-extraction/06-CONTEXT.md
@app/events/routes.py
@app/templates/calendar.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OCR service and API endpoint</name>
  <files>app/events/ocr.py, app/events/routes.py, requirements.txt</files>
  <acceptance_criteria>
    - New OCR service parses image bytes into event-shaped payload using EasyOCR when available.
    - New `/api/events/ocr-parse` endpoint accepts uploaded image and returns structured OCR parse response.
    - Endpoint degrades gracefully with actionable errors when OCR runtime is unavailable.
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_events_api.py -k "ocr or parse"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Integrate OCR flow into quick-add modal and add integration tests</name>
  <files>app/templates/partials/quick_add_modal.html, app/templates/calendar.html, tests/test_calendar_views.py</files>
  <acceptance_criteria>
    - Quick Add includes Scan Image action and hidden file input.
    - OCR parse success routes to review phase with confidence note and editable fields.
    - OCR parse failures route to fallback with raw OCR text visible.
    - Calendar view tests assert OCR controls and endpoint wiring markers.
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_calendar_views.py</automated>
  </verify>
</task>

</tasks>

<verification>
Run focused suites covering OCR API and quick-add UI integration:
`.venv\Scripts\python.exe -m pytest -q tests/test_events_api.py tests/test_calendar_views.py tests/test_nlp.py`
</verification>

<success_criteria>
- OCR-01: image upload and extraction endpoint works from quick-add flow.
- OCR-02: extracted data includes confidence and review-before-save path.
- OCR-03: user can edit extracted fields before save (reused review editor).
</success_criteria>

<output>
After completion, create `.planning/phases/06-image-ocr-event-extraction/06-01-SUMMARY.md` and `.planning/phases/06-image-ocr-event-extraction/06-VERIFICATION.md`.
</output>