---
phase: 05-natural-language-input
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/events/nlp.py
  - app/events/routes.py
  - tests/test_nlp.py
  - tests/test_events_api.py
autonomous: true
gap_closure: true
requirements: [NLP-01]
must_haves:
  truths:
    - "Phrase 'dentist Thursday 2pm' parses into title + future datetime without error"
    - "Relative phrases like 'in 3 days' do not misinterpret numeric day offsets as clock hour"
    - "Parse endpoint passes user timezone context into NLPService instead of hardcoded UTC"
  artifacts:
    - path: "app/events/nlp.py"
      provides: "weekday anchor parsing and relative-date fixes"
      min_lines: 200
    - path: "app/events/routes.py"
      provides: "timezone propagation from authenticated user context"
      exports: ["parse_event"]
    - path: "tests/test_nlp.py"
      provides: "regression coverage for weekday anchors and relative-date behavior"
      min_lines: 120
    - path: "tests/test_events_api.py"
      provides: "API coverage for timezone propagation in parse endpoint"
      min_lines: 120
  key_links:
    - from: "app/events/routes.py"
      to: "app/events/nlp.py"
      via: "NLPService.parse(..., timezone=...)"
      pattern: "parse\(.*timezone="
    - from: "tests/test_nlp.py"
      to: "app/events/nlp.py"
      via: "direct NLPService.parse assertions"
      pattern: "dentist Thursday 2pm|in 3 days"
---

<objective>
Close core NLP parsing reliability gaps by fixing weekday-anchor/relative-date parsing and timezone propagation through the parse API.

Purpose: Restore NLP-01 baseline behavior required by Phase 05 goal
Output: Parser fixes in `nlp.py`, API timezone wiring in `routes.py`, and passing regression tests
</objective>

<execution_context>
@.planning/phases/05-natural-language-input/05-VERIFICATION.md
@.planning/phases/05-natural-language-input/05-CONTEXT.md
</execution_context>

<context>
@app/events/nlp.py
@app/events/routes.py
@tests/test_nlp.py
@tests/test_events_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock failing NLP behaviors with explicit regression tests</name>
  <files>tests/test_nlp.py, tests/test_events_api.py</files>
  <read_first>
    - tests/test_nlp.py
    - tests/test_events_api.py
    - app/events/nlp.py
    - app/events/routes.py
    - .planning/phases/05-natural-language-input/05-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: `NLPService.parse("dentist Thursday 2pm")` returns no errors and non-empty `start_at`
    - Test 2: `NLPService.parse("meeting in 3 days")` sets date offset +3 days and keeps default hour behavior (not hour=3)
    - Test 3: parse API passes non-UTC user timezone into parser invocation context
  </behavior>
  <action>Add/adjust tests so the above scenarios are explicit and initially fail against current code. Use deterministic context dates/timezones in tests to avoid midnight flakiness.</action>
  <acceptance_criteria>
    - `tests/test_nlp.py` contains assertions for `dentist Thursday 2pm`
    - `tests/test_nlp.py` contains assertions for `in 3 days` date delta semantics
    - `tests/test_events_api.py` contains assertion proving timezone value is propagated (not hardcoded UTC)
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_nlp.py tests/test_events_api.py -k "dentist or in_3_days or parse"</automated>
  </verify>
  <done>Regression tests for all three failure modes exist and fail before implementation changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parser and endpoint fixes to satisfy regression suite</name>
  <files>app/events/nlp.py, app/events/routes.py</files>
  <read_first>
    - app/events/nlp.py
    - app/events/routes.py
    - tests/test_nlp.py
    - tests/test_events_api.py
  </read_first>
  <behavior>
    - Plain weekday anchors are recognized (Thursday/Friday without next/this/last)
    - Relative-day phrase parsing does not treat day-count numerics as clock time
    - Parse endpoint forwards user/session timezone value to parser
  </behavior>
  <action>Update weekday regex and anchor resolution in parser, correct `in N days` token handling order, and replace hardcoded UTC in parse route with timezone from authenticated user/session payload while retaining UTC fallback when unavailable.</action>
  <acceptance_criteria>
    - `app/events/nlp.py` contains plain-weekday branch handling
    - `app/events/nlp.py` contains explicit path for `in\s+\d+\s+days`
    - `app/events/routes.py` no longer hardcodes timezone to UTC before parse call
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_nlp.py tests/test_events_api.py</automated>
  </verify>
  <done>All NLP parser/API tests pass for weekday-anchor, relative-date, and timezone propagation behaviors.</done>
</task>

</tasks>

<verification>
Run targeted NLP and parse API tests, then perform a direct parse smoke-check for `dentist Thursday 2pm` to confirm no error response.
</verification>

<success_criteria>
- `dentist Thursday 2pm` parse path works in automated tests
- `in 3 days` date semantics are correct and stable
- parse route timezone propagation is verified by tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-natural-language-input/05-04-SUMMARY.md`
</output>
