---
phase: 05-natural-language-input
plan: 05
type: execute
wave: 2
depends_on: [05-04]
files_modified:
  - app/templates/calendar.html
  - app/templates/partials/quick_add_modal.html
  - tests/test_calendar_views.py
autonomous: true
gap_closure: true
requirements: [NLP-01, NLP-02, NLP-03]
must_haves:
  truths:
    - "Ambiguous month/day parses present a clear year-choice confirmation before save"
    - "User can choose the intended year and continue through review/save flow"
    - "Quick-add flow keeps existing accessibility and fallback behavior while adding disambiguation"
  artifacts:
    - path: "app/templates/calendar.html"
      provides: "year-disambiguation state handling in quick-add orchestration"
      min_lines: 250
    - path: "app/templates/partials/quick_add_modal.html"
      provides: "UI controls for ambiguity prompt with year options"
      min_lines: 180
    - path: "tests/test_calendar_views.py"
      provides: "integration coverage for disambiguation UX path"
      min_lines: 150
  key_links:
    - from: "app/templates/calendar.html"
      to: "app/templates/partials/quick_add_modal.html"
      via: "disambiguation phase state transition"
      pattern: "qa-ambiguity|year-choice|showPhase"
    - from: "tests/test_calendar_views.py"
      to: "app/templates/calendar.html"
      via: "assertions for ambiguity prompt rendering and save flow continuation"
      pattern: "ambigu|year|quick add"
---

<objective>
Close the year-disambiguation gap by adding an explicit ambiguity confirmation step in quick-add review when month/day input lacks year.

Purpose: Complete NLP-01 user-facing confidence path and prevent silent incorrect-year assumptions
Output: Ambiguity UI path in quick-add modal with integration tests
</objective>

<execution_context>
@.planning/phases/05-natural-language-input/05-VERIFICATION.md
@.planning/phases/05-natural-language-input/05-04-PLAN.md
</execution_context>

<context>
@app/templates/calendar.html
@app/templates/partials/quick_add_modal.html
@tests/test_calendar_views.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration tests for year-disambiguation quick-add flow</name>
  <files>tests/test_calendar_views.py</files>
  <read_first>
    - tests/test_calendar_views.py
    - app/templates/calendar.html
    - app/templates/partials/quick_add_modal.html
    - .planning/phases/05-natural-language-input/05-VERIFICATION.md
  </read_first>
  <behavior>
    - Parse result with ambiguity metadata triggers year-choice UI
    - Choosing a year updates review start/end fields consistently
    - Save remains blocked until year selection is made for ambiguous parses
  </behavior>
  <action>Add UI-integration tests that assert ambiguity panel IDs/classes/text markers, year option controls, and gating behavior prior to save. Reuse existing quick-add test style in this suite.</action>
  <acceptance_criteria>
    - `tests/test_calendar_views.py` contains tests for ambiguity prompt visibility
    - `tests/test_calendar_views.py` contains tests for year selection applying to review values
    - `tests/test_calendar_views.py` contains save-gating assertion for unresolved ambiguity
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_calendar_views.py -k "ambigu or year or quick_add"</automated>
  </verify>
  <done>Tests clearly define expected disambiguation flow and fail before UI implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ambiguity confirmation UI and state transitions</name>
  <files>app/templates/partials/quick_add_modal.html, app/templates/calendar.html</files>
  <read_first>
    - app/templates/partials/quick_add_modal.html
    - app/templates/calendar.html
    - tests/test_calendar_views.py
  </read_first>
  <behavior>
    - Ambiguous parse shows prompt like "Did you mean 2026 or 2027?"
    - User selection applies chosen year to parsed start/end fields
    - Flow returns to review and allows normal save/save-another actions
  </behavior>
  <action>Add an ambiguity subsection in modal markup with explicit year-choice buttons and wire it in quick-add JS state machine. When parser metadata indicates ambiguity, route to ambiguity phase first, then populate review using chosen year and preserve accessibility announcements.</action>
  <acceptance_criteria>
    - `app/templates/partials/quick_add_modal.html` contains ambiguity prompt and year action controls
    - `app/templates/calendar.html` contains ambiguity phase handling branch before review save path
    - existing fallback and a11y live-region markers remain present
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_calendar_views.py</automated>
  </verify>
  <done>Ambiguity UI path works end-to-end and does not regress existing quick-add behaviors.</done>
</task>

</tasks>

<verification>
Execute full calendar view test file to ensure quick-add orchestration, accessibility, and new ambiguity branch all pass together.
</verification>

<success_criteria>
- Ambiguous month/day parsing no longer silently auto-selects year in UX flow
- User is explicitly prompted to choose intended year before saving
- Existing quick-add/fallback/a11y flows remain green in integration tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-natural-language-input/05-05-SUMMARY.md`
</output>
