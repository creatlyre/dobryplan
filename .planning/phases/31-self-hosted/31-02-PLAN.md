---
phase: 31-self-hosted
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - self-hosted/README.md
  - self-hosted/UPGRADE.md
  - self-hosted/CHANGELOG-SELFHOSTED.md
  - tests/test_licensing.py
autonomous: true
requirements: [SHS-03, SHS-04]

must_haves:
  truths:
    - "Buyer-facing setup guide covers prerequisites through first login"
    - "Upgrade instructions document pull + restart + migration procedure"
    - "Self-hosted changelog exists with initial v4.0 entry"
    - "License key generation and validation have automated tests"
  artifacts:
    - path: "self-hosted/README.md"
      provides: "Complete buyer-facing setup guide"
      contains: "docker compose up"
    - path: "self-hosted/UPGRADE.md"
      provides: "Version upgrade procedure"
      contains: "docker compose pull"
    - path: "self-hosted/CHANGELOG-SELFHOSTED.md"
      provides: "Self-hosted release notes"
      contains: "v4.0"
    - path: "tests/test_licensing.py"
      provides: "License key unit tests"
      contains: "test_generate"
  key_links:
    - from: "self-hosted/README.md"
      to: "self-hosted/docker-compose.yml"
      via: "Step-by-step instructions reference compose file"
      pattern: "docker compose"
    - from: "self-hosted/README.md"
      to: "self-hosted/.env.template"
      via: "Guide references env template for configuration"
      pattern: ".env.template"
    - from: "self-hosted/UPGRADE.md"
      to: "self-hosted/CHANGELOG-SELFHOSTED.md"
      via: "Upgrade instructions reference changelog"
      pattern: "CHANGELOG"
---

<objective>
Create the buyer-facing documentation and test coverage for the self-hosted distribution package.

Purpose: SHS-03 requires a versioned upgrade path, SHS-04 requires a complete setup guide. Tests ensure the licensing module is correct and remains working.
Output: Setup guide (README), upgrade instructions, changelog, and licensing tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-self-hosted/31-CONTEXT.md
@.planning/phases/31-self-hosted/31-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 -->

From app/licensing/keys.py:
```python
def generate_license_key(secret: str) -> str: ...
def validate_license_key(key: str, secret: str) -> bool: ...
```

From app/licensing/middleware.py:
```python
class LicenseCheckMiddleware(BaseHTTPMiddleware):
    # Injects warning banner for invalid/missing keys in self-hosted mode
```

From self-hosted/docker-compose.yml:
```yaml
services:
  app:    # FastAPI app with gunicorn
  db:     # PostgreSQL (supabase/postgres)
  rest:   # PostgREST (optional, for Supabase client compat)
  caddy:  # Reverse proxy with auto-HTTPS
```

From config.py:
```python
SYNCO_LICENSE_KEY: str = ""
SYNCO_LICENSE_SECRET: str = ""
ENVIRONMENT: str = "development"  # includes "self-hosted"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: License key tests</name>
  <files>tests/test_licensing.py</files>
  <read_first>app/licensing/keys.py, app/licensing/middleware.py, tests/conftest.py</read_first>
  <action>
Create `tests/test_licensing.py` with comprehensive license key tests:

1. **Key generation tests:**
   - `test_generate_license_key_format`: Generated key starts with `SYNCO-`, has expected dash-separated format
   - `test_generate_license_key_unique`: Two sequential keys are different
   - `test_generate_license_key_deterministic_validation`: Generated key passes validation with same secret

2. **Key validation tests:**
   - `test_validate_valid_key`: A key generated with secret X validates with secret X
   - `test_validate_wrong_secret`: A key generated with secret X fails with secret Y
   - `test_validate_malformed_key`: Random strings, empty string, "SYNCO-", partial keys all return False
   - `test_validate_tampered_key`: Flipping a character in a valid key causes validation failure
   - `test_validate_empty_key`: Empty string returns False
   - `test_validate_none_handling`: Ensure None is handled gracefully (if the function accepts Optional[str])

3. **Middleware tests (using TestClient):**
   - `test_license_middleware_skips_non_selfhosted`: When ENVIRONMENT != "self-hosted", no banner injected
   - `test_license_middleware_valid_key_no_banner`: When ENVIRONMENT=self-hosted and valid key, no banner
   - `test_license_middleware_invalid_key_shows_banner`: When ENVIRONMENT=self-hosted and invalid key, banner text present in HTML response
   - `test_license_middleware_skips_health_endpoints`: /health and /health/ready never get banner
   - `test_license_middleware_skips_api_json`: JSON API responses never get banner injected

Use env var overrides in tests (monkeypatch or os.environ) to control ENVIRONMENT, SYNCO_LICENSE_KEY, SYNCO_LICENSE_SECRET.
  </action>
  <verify>
    <automated>cd c:\Users\Wojciech\Documents\Python\CalendarPlanner; .venv\Scripts\python -m pytest tests/test_licensing.py -v 2>&1 | Select-Object -Last 20</automated>
  </verify>
  <done>All license key tests pass — generation, validation, middleware behavior for all environments.</done>
</task>

<task type="auto">
  <name>Task 2: Setup guide, upgrade docs, and changelog</name>
  <files>self-hosted/README.md, self-hosted/UPGRADE.md, self-hosted/CHANGELOG-SELFHOSTED.md</files>
  <read_first>self-hosted/docker-compose.yml, self-hosted/.env.template, self-hosted/Caddyfile, config.py, MONETIZATION.md</read_first>
  <action>
1. Create `self-hosted/README.md` — Complete buyer-facing setup guide:

   ## Structure:
   - **Prerequisites**: Docker & Docker Compose, a domain name (for HTTPS), firewall open on 80/443
   - **Quick Start** (5 steps):
     1. Clone or extract the self-hosted package
     2. Copy `.env.template` to `.env` and fill in values (SECRET_KEY, DB_ENCRYPTION_KEY, SYNCO_LICENSE_KEY, POSTGRES_PASSWORD, SITE_DOMAIN)
     3. `docker compose up -d`
     4. Wait for health checks to pass: `docker compose ps`
     5. Open `https://your-domain.com` and create first user
   - **Configuration Reference**: Table of all env vars with descriptions, required/optional, defaults
   - **Google Calendar Integration** (optional): How to get OAuth client ID/secret, set redirect URI
   - **Email Notifications** (optional): SMTP configuration
   - **HTTPS / Custom Domain**: Caddy auto-HTTPS with Let's Encrypt (just set SITE_DOMAIN)
   - **Backup & Restore**: `docker compose exec db pg_dump -U postgres synco > backup.sql`
   - **Troubleshooting**: Common issues (port conflicts, license key errors, DB connection, Google OAuth redirect mismatch)
   - **Support**: Contact licensing@synco.app

2. Create `self-hosted/UPGRADE.md`:
   - **Before You Upgrade**: Backup database, check CHANGELOG-SELFHOSTED.md for breaking changes
   - **Standard Upgrade**:
     1. `docker compose pull` (or rebuild: `docker compose build --no-cache app`)
     2. `docker compose down`
     3. `docker compose up -d`
     4. Verify: `docker compose ps` — all services healthy
   - **Database Migrations**: Mention that migrations run automatically at startup if applicable (or manual steps if needed)
   - **Rollback**: Stop, restore backup, start previous version
   - **Version History**: Link to CHANGELOG-SELFHOSTED.md

3. Create `self-hosted/CHANGELOG-SELFHOSTED.md`:
   ```markdown
   # Synco Self-Hosted Changelog

   All notable changes to the self-hosted distribution package.

   ## v4.0.0 — 2026-03-23

   ### Initial Release

   - Docker Compose production package (app + PostgreSQL + PostgREST + Caddy)
   - HMAC-based license key verification (offline, no phone-home)
   - Automatic HTTPS via Caddy + Let's Encrypt
   - Full feature set: calendar, budget, shopping, notifications, dashboard
   - Polish/English localization
   - Google Calendar sync support
   - Stripe billing support (optional for self-hosted)

   ### Prerequisites

   - Docker 24+ and Docker Compose v2
   - A domain name pointed to your server (for HTTPS)
   - 1GB RAM minimum, 2GB recommended

   ### Known Limitations

   - Single-instance deployment only (one license key = one deployment)
   - No automatic update notifications (check this changelog)
   ```
  </action>
  <verify>
    <automated>cd c:\Users\Wojciech\Documents\Python\CalendarPlanner; .venv\Scripts\python -c "
import os
files = ['self-hosted/README.md', 'self-hosted/UPGRADE.md', 'self-hosted/CHANGELOG-SELFHOSTED.md']
for f in files:
    assert os.path.exists(f), f'Missing: {f}'
    content = open(f).read()
    assert len(content) > 200, f'Too short: {f} ({len(content)} chars)'
    print(f'OK: {f} ({len(content)} chars)')
readme = open('self-hosted/README.md').read()
assert 'docker compose up' in readme, 'README missing docker compose up'
assert '.env.template' in readme, 'README missing .env.template reference'
assert 'SYNCO_LICENSE_KEY' in readme, 'README missing license key reference'
upgrade = open('self-hosted/UPGRADE.md').read()
assert 'docker compose pull' in upgrade, 'UPGRADE missing pull instructions'
assert 'backup' in upgrade.lower(), 'UPGRADE missing backup mention'
print('OK: All documentation complete and valid')
"</automated>
  </verify>
  <done>Setup guide covers prerequisites through first login. Upgrade doc covers pull+restart+rollback. Changelog has v4.0 initial entry. All docs reference correct file paths and commands.</done>
</task>

</tasks>

<verification>
- All license key tests pass
- Setup guide is comprehensive (prerequisites, quick start, config reference, troubleshooting)
- Upgrade path documents backup, pull, restart, rollback procedures
- Changelog has v4.0.0 initial release entry
- All existing 336+ tests still pass
</verification>

<success_criteria>
- tests/test_licensing.py has 10+ tests, all passing
- self-hosted/README.md is a complete, reproducible setup guide
- self-hosted/UPGRADE.md documents standard upgrade and rollback
- self-hosted/CHANGELOG-SELFHOSTED.md has initial v4.0 entry
- SHS-03 and SHS-04 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/31-self-hosted/31-02-SUMMARY.md`
</output>
