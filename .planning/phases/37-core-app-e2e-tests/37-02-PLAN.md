---
phase: 37-core-app-e2e-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/tests/test_dashboard.spec.ts
  - e2e/tests/test_notifications.spec.ts
autonomous: true
requirements:
  - DASH-E2E-01
  - DASH-E2E-02
  - DASH-E2E-03
  - NOTF-E2E-01
  - NOTF-E2E-02

must_haves:
  truths:
    - "Dashboard loads as home page after login (authenticated / redirects to /dashboard)"
    - "Today's events section is visible on the dashboard"
    - "Budget snapshot area and quick-add chooser button are visible on the dashboard"
    - "Notification bell icon is visible in the navbar for authenticated users"
    - "Clicking the bell loads notification dropdown content"
  artifacts:
    - path: "e2e/tests/test_dashboard.spec.ts"
      provides: "Dashboard E2E tests (page load, today's events, budget snapshot, quick-add)"
      contains: "test.describe('Dashboard'"
    - path: "e2e/tests/test_notifications.spec.ts"
      provides: "Notification UI E2E tests (bell icon, dropdown load)"
      contains: "test.describe('Notifications'"
  key_links:
    - from: "e2e/tests/test_dashboard.spec.ts"
      to: "/dashboard"
      via: "page.goto for dashboard"
      pattern: "page\\.goto.*dashboard"
    - from: "e2e/tests/test_dashboard.spec.ts"
      to: "/"
      via: "page.goto root to verify redirect"
      pattern: "page\\.goto.*'/'"
    - from: "e2e/tests/test_notifications.spec.ts"
      to: "#notification-bell"
      via: "locator for bell button"
      pattern: "#notification-bell"
    - from: "e2e/tests/test_notifications.spec.ts"
      to: "/notifications/dropdown"
      via: "HTMX load triggered by bell click"
      pattern: "notifications/dropdown"
---

<objective>
Create E2E tests for dashboard rendering (page load, sections, quick-add) and notification UI (bell icon, dropdown).

Purpose: Verify the dashboard home page and notification interface work correctly in a real browser.
Output: Two Playwright test files covering 5 requirements (DASH-E2E-01 through DASH-E2E-03, NOTF-E2E-01, NOTF-E2E-02).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-core-app-e2e-tests/37-CONTEXT.md
@.planning/phases/37-core-app-e2e-tests/37-RESEARCH.md
@.planning/phases/36-e2e-test-infrastructure/36-CONTEXT.md

<interfaces>
<!-- Phase 36 Playwright config (e2e/playwright.config.ts) -->
Projects: setup, free, pro, family-plus
Storage states: e2e/playwright/.auth/{free,pro,family-plus}.json
Test dir for role projects: ./tests (relative to e2e/)
Base URL: process.env.E2E_BASE_URL || 'https://synco-production-e9da.up.railway.app'

<!-- Root route (main.py line 263) -->
GET / → if authenticated: redirect 302 to /dashboard
GET / → if NOT authenticated: render landing.html

<!-- Dashboard (app/templates/dashboard.html) -->
Today's events section: h2 with t('dashboard.today_events') text
Quick-add section: text t('dashboard.quick_event')
Full event link: a[href="/calendar?open=event-entry"] with t('dashboard.full_event')
Left column: events section
Budget snapshot section present in template

<!-- Dashboard route (app/dashboard/routes.py) -->
Router prefix: /dashboard
GET /dashboard → renders dashboard.html (requires get_current_user)

<!-- Navbar notification bell (app/templates/base.html) -->
Bell container: div#notification-bell-container (class="relative")
Bell button: button#notification-bell
  - hx-get="/notifications/dropdown" (loads dropdown on click)
  - hx-target="#notification-dropdown-content"
  - hx-trigger="click"
Badge: span inside bell — loaded via hx-get="/notifications/badge" hx-trigger="load"

<!-- Notification dropdown (app/notifications/views.py) -->
GET /notifications/dropdown → renders partials/notification_dropdown.html (HTMX partial)
GET /notifications/badge → returns badge HTML or empty string
Router prefix: /notifications
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard E2E tests</name>
  <files>e2e/tests/test_dashboard.spec.ts</files>
  <read_first>
    - e2e/playwright.config.ts (project config, baseURL, storageState)
    - app/templates/dashboard.html (section structure: today_events, quick_event, full_event, budget, week preview)
    - app/dashboard/routes.py (GET /dashboard route, template context variables)
    - main.py lines 263-270 (GET / redirect behavior for authenticated users)
    - .planning/phases/37-core-app-e2e-tests/37-CONTEXT.md (dashboard verification approach: read-only)
  </read_first>
  <action>
Create `e2e/tests/test_dashboard.spec.ts` with these tests using `import { test, expect } from '@playwright/test'`:

**test.describe('Dashboard', () => { ... })** containing:

1. **'loads as home page for authenticated user'** (DASH-E2E-01):
   - `await page.goto('/')`
   - Wait for redirect to dashboard: `await page.waitForURL('**/dashboard**')`
   - Assert URL: `await expect(page).toHaveURL(/\/dashboard/)`
   - Assert page is not empty: `await expect(page.locator('body')).not.toBeEmpty()`

2. **'shows today events section'** (DASH-E2E-02):
   - `await page.goto('/dashboard')`
   - Wait for page to load: `await page.waitForLoadState('networkidle')`
   - Assert today's events heading is visible. The heading uses i18n key `dashboard.today_events` — the actual Polish text may vary. Look for the h2 element with class `text-section-title` in the events area:
     `await expect(page.locator('h2.text-section-title').first()).toBeVisible()`
   - Alternative: check that the events card section exists. Dashboard has a card container for today's events.

3. **'shows budget snapshot and quick-add'** (DASH-E2E-03):
   - `await page.goto('/dashboard')`
   - Wait for page to load: `await page.waitForLoadState('networkidle')`
   - Assert quick-add chooser button exists in the navbar (the base.html template has a quick-add button):
     Check for the "Szybkie dodawanie" (quick-add) button/element in the page, or the link to `/calendar?open=event-entry`
   - Assert the calendar event link exists: `await expect(page.locator('a[href="/calendar?open=event-entry"]')).toBeVisible()`
   - Assert the page has multiple sections (verifying dashboard is fully rendered, not just a shell):
     `const sections = await page.locator('.glass').count()` — expect at least 2 glass cards

**All tests use the default `page` fixture** with stored auth from the `pro` project. No manual login needed.

**Note on Polish locale:** The app's default language is Polish. Text content assertions should either use Polish strings or avoid text matching entirely (prefer selector-based assertions).
  </action>
  <verify>
    <automated>npx playwright test e2e/tests/test_dashboard.spec.ts --config=e2e/playwright.config.ts --project=pro 2>&1 | Select-Object -Last 20</automated>
  </verify>
  <acceptance_criteria>
    - e2e/tests/test_dashboard.spec.ts contains `test.describe('Dashboard'`
    - e2e/tests/test_dashboard.spec.ts contains `test('loads as home page for authenticated user'`
    - e2e/tests/test_dashboard.spec.ts contains `test('shows today events section'`
    - e2e/tests/test_dashboard.spec.ts contains `test('shows budget snapshot and quick-add'`
    - e2e/tests/test_dashboard.spec.ts contains `page.goto('/')`
    - e2e/tests/test_dashboard.spec.ts contains `page.goto('/dashboard')`
    - e2e/tests/test_dashboard.spec.ts contains `waitForURL('**/dashboard**')` or `toHaveURL(/\/dashboard/)`
    - e2e/tests/test_dashboard.spec.ts contains `a[href="/calendar?open=event-entry"]`
    - e2e/tests/test_dashboard.spec.ts has NO data mutation calls (no form submissions, no POSTs)
    - All 3 dashboard tests pass: `npx playwright test e2e/tests/test_dashboard.spec.ts --config=e2e/playwright.config.ts --project=pro` exits 0
  </acceptance_criteria>
  <done>3 dashboard E2E tests pass: root redirects to /dashboard, today's events section visible, budget snapshot and quick-add visible</done>
</task>

<task type="auto">
  <name>Task 2: Notification UI E2E tests</name>
  <files>e2e/tests/test_notifications.spec.ts</files>
  <read_first>
    - e2e/playwright.config.ts (project config, baseURL)
    - app/templates/base.html lines 57-75 (notification bell HTML: #notification-bell-container, #notification-bell, hx-get patterns)
    - app/notifications/views.py (GET /notifications/badge, GET /notifications/dropdown routes)
    - app/templates/partials/notification_dropdown.html (dropdown content structure)
    - .planning/phases/37-core-app-e2e-tests/37-CONTEXT.md (notification verification: UI-presence only, no content assertions)
  </read_first>
  <action>
Create `e2e/tests/test_notifications.spec.ts` with these tests using `import { test, expect } from '@playwright/test'`:

**test.describe('Notifications', () => { ... })** containing:

1. **'bell icon visible in navbar'** (NOTF-E2E-01):
   - `await page.goto('/dashboard')`
   - Wait for page to load fully: `await page.waitForLoadState('networkidle')`
   - Assert bell container exists: `await expect(page.locator('#notification-bell-container')).toBeVisible()`
   - Assert bell button exists: `await expect(page.locator('#notification-bell')).toBeVisible()`

2. **'bell click loads notification dropdown'** (NOTF-E2E-02):
   - `await page.goto('/dashboard')`
   - Wait for page to load: `await page.waitForLoadState('networkidle')`
   - Assert bell button is visible: `await expect(page.locator('#notification-bell')).toBeVisible()`
   - Click the bell: `await page.locator('#notification-bell').click()`
   - Wait for HTMX to load the dropdown content. The bell has `hx-get="/notifications/dropdown"` which loads HTML into the dropdown target. Wait for the network response:
     `await page.waitForResponse(resp => resp.url().includes('/notifications/dropdown') && resp.status() === 200)`
   - Assert dropdown content container appeared — check that some HTML was loaded into the dropdown target area. The dropdown template (`partials/notification_dropdown.html`) renders a list or an empty state.
   - Verify the dropdown area has content: `await expect(page.locator('#notification-bell-container').locator('div').nth(1)).toBeVisible()` or check for the HTMX-swapped content.
   - **No assertions on notification count or content** — per CONTEXT.md, notification content depends on uncontrollable partner activity.

**All tests use the default `page` fixture** with stored auth from the `pro` project.

**HTMX interaction:** The bell button has `hx-get="/notifications/dropdown"` with `hx-trigger="click"`. After click, Playwright should wait for the `/notifications/dropdown` response before asserting the dropdown content appeared.
  </action>
  <verify>
    <automated>npx playwright test e2e/tests/test_notifications.spec.ts --config=e2e/playwright.config.ts --project=pro 2>&1 | Select-Object -Last 20</automated>
  </verify>
  <acceptance_criteria>
    - e2e/tests/test_notifications.spec.ts contains `test.describe('Notifications'`
    - e2e/tests/test_notifications.spec.ts contains `test('bell icon visible in navbar'`
    - e2e/tests/test_notifications.spec.ts contains `test('bell click loads notification dropdown'`
    - e2e/tests/test_notifications.spec.ts contains `page.locator('#notification-bell-container')`
    - e2e/tests/test_notifications.spec.ts contains `page.locator('#notification-bell')`
    - e2e/tests/test_notifications.spec.ts contains `click()` on notification bell
    - e2e/tests/test_notifications.spec.ts contains `waitForResponse` for `/notifications/dropdown`
    - e2e/tests/test_notifications.spec.ts has NO assertions on notification count or specific notification text content
    - All 2 notification tests pass: `npx playwright test e2e/tests/test_notifications.spec.ts --config=e2e/playwright.config.ts --project=pro` exits 0
  </acceptance_criteria>
  <done>2 notification E2E tests pass: bell icon visible in navbar, bell click loads dropdown content</done>
</task>

</tasks>

<verification>
```bash
# Run all tests from this plan
npx playwright test e2e/tests/test_dashboard.spec.ts e2e/tests/test_notifications.spec.ts --config=e2e/playwright.config.ts --project=pro

# Verify test count
npx playwright test e2e/tests/test_dashboard.spec.ts e2e/tests/test_notifications.spec.ts --config=e2e/playwright.config.ts --project=pro --list
```
</verification>

<success_criteria>
- 3 dashboard tests pass (home redirect, events section, budget + quick-add)
- 2 notification tests pass (bell visible, dropdown loads)
- All tests are read-only — no data mutations in production
- Tests use stored auth from Phase 36 infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/37-core-app-e2e-tests/37-02-SUMMARY.md`
</output>
