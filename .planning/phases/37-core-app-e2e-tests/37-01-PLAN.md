---
phase: 37-core-app-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/tests/test_auth.spec.ts
  - e2e/tests/test_calendar.spec.ts
autonomous: true
requirements:
  - AUTH-E2E-01
  - AUTH-E2E-02
  - AUTH-E2E-03
  - AUTH-E2E-04
  - CAL-E2E-01
  - CAL-E2E-02
  - CAL-E2E-03

must_haves:
  truths:
    - "Valid login credentials redirect to /dashboard"
    - "Invalid login credentials show a visible error message on the login page"
    - "Register page renders a form with email and password fields"
    - "Logout clears session and redirects to /auth/login"
    - "Calendar month grid renders day cells inside #month-grid"
    - "Month navigation arrows change the displayed month"
    - "Clicking the event entry button opens the event entry modal"
  artifacts:
    - path: "e2e/tests/test_auth.spec.ts"
      provides: "Auth flow E2E tests (login, invalid login, register, logout)"
      contains: "test.describe('Auth'"
    - path: "e2e/tests/test_calendar.spec.ts"
      provides: "Calendar view E2E tests (month grid, navigation, event modal)"
      contains: "test.describe('Calendar'"
  key_links:
    - from: "e2e/tests/test_auth.spec.ts"
      to: "/auth/login"
      via: "page.goto and form interaction"
      pattern: "page\\.goto.*auth/login"
    - from: "e2e/tests/test_auth.spec.ts"
      to: "/auth/register"
      via: "page.goto for register page"
      pattern: "page\\.goto.*auth/register"
    - from: "e2e/tests/test_auth.spec.ts"
      to: "/auth/logout"
      via: "page.goto for logout"
      pattern: "page\\.goto.*auth/logout"
    - from: "e2e/tests/test_calendar.spec.ts"
      to: "/calendar"
      via: "page.goto for calendar page"
      pattern: "page\\.goto.*calendar"
    - from: "e2e/tests/test_calendar.spec.ts"
      to: "#month-grid"
      via: "locator for HTMX-loaded month grid"
      pattern: "#month-grid"
---

<objective>
Create E2E tests for auth flow (login, invalid login, register form, logout) and calendar views (month grid, navigation, event modal).

Purpose: Verify the most critical user journeys — authentication and calendar rendering — work in a real browser against the live deployment.
Output: Two Playwright test files covering 7 requirements (AUTH-E2E-01 through AUTH-E2E-04, CAL-E2E-01 through CAL-E2E-03).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-core-app-e2e-tests/37-CONTEXT.md
@.planning/phases/37-core-app-e2e-tests/37-RESEARCH.md
@.planning/phases/36-e2e-test-infrastructure/36-CONTEXT.md

<interfaces>
<!-- Phase 36 Playwright config (e2e/playwright.config.ts) -->
Projects: setup, free, pro, family-plus
Storage states: e2e/playwright/.auth/{free,pro,family-plus}.json
Test dir for role projects: ./tests (relative to e2e/)
Base URL: process.env.E2E_BASE_URL || 'https://synco-production-e9da.up.railway.app'
Timeout: 30s per test, 15s navigation timeout

<!-- Login form (app/templates/login.html) -->
Form: #loginForm (novalidate)
Email: input#email (type="email", name="email")
Password: input#password (type="password", name="password")
Submit: .btn-primary button
Error: div#error (class="error-msg") — shown on login failure
Login POST: fetch('/auth/password-login', {method:'POST', body: JSON.stringify({email, password})})
On success: window.location.href = '/'
On failure: error div made visible with error text

<!-- Register form (app/templates/register.html) -->
Form: #registerForm (novalidate)
Email: input#email (type="email")
Password: input#password (type="password")
Submit: .btn-primary button

<!-- Auth routes (app/auth/routes.py) -->
GET /auth/login → renders login.html
POST /auth/password-login → JSON {email, password} → 200 + cookies on success, 401 on failure
GET /auth/register → renders register.html
POST /auth/register → JSON {email, password} → creates user
GET /auth/logout → deletes session + supabase_refresh cookies → redirect to /auth/login (302)
POST /auth/logout → same, 303 redirect

<!-- Root route (main.py) -->
GET / → if authenticated: redirect 302 to /dashboard; if not: render landing.html

<!-- Calendar (app/templates/calendar.html) -->
Month grid container: div#month-grid — loads via hx-get="/calendar/month?year=...&month=..." on page load
Day events container: div#day-events — loads via hx-get="/calendar/day?year=...&month=...&day=..." on page load
Event entry button: button#event-entry-open-btn
Event entry modal: included from partials/event_entry_modal.html

<!-- Calendar routes (app/views/calendar_routes.py) -->
GET /calendar → renders calendar.html (requires auth)
GET /calendar/month?year=N&month=N → HTMX partial with month grid cells
GET /calendar/day?year=N&month=N&day=N → HTMX partial with day events
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth flow E2E tests</name>
  <files>e2e/tests/test_auth.spec.ts</files>
  <read_first>
    - e2e/playwright.config.ts (project names, storageState paths, baseURL)
    - app/templates/login.html (form selectors: #loginForm, #email, #password, .btn-primary, #error)
    - app/templates/register.html (form selectors: #registerForm, #email, #password)
    - app/auth/routes.py (GET /auth/login, POST /auth/password-login, GET /auth/logout, GET /auth/register)
    - .planning/phases/37-core-app-e2e-tests/37-CONTEXT.md (test data strategy, auth test scope decisions)
  </read_first>
  <action>
Create `e2e/tests/test_auth.spec.ts` with these tests using `import { test, expect } from '@playwright/test'`:

**test.describe('Auth', () => { ... })** containing:

1. **'valid login redirects to dashboard'** (AUTH-E2E-01):
   - Use a fresh browser context (no storage state) by creating a new context: `const context = await browser.newContext(); const page = await context.newPage();`
   - `await page.goto('/auth/login')`
   - `await expect(page.locator('#loginForm')).toBeVisible()`
   - Fill `#email` with `process.env.E2E_PRO_EMAIL!`
   - Fill `#password` with `process.env.E2E_PRO_PASSWORD!`
   - Click `.btn-primary`
   - Wait for redirect: `await page.waitForURL('**/dashboard**')`
   - Assert URL contains `/dashboard`: `await expect(page).toHaveURL(/\/dashboard/)`
   - Close context: `await context.close()`

2. **'invalid login shows error message'** (AUTH-E2E-02):
   - Use a fresh browser context (no storage state)
   - `await page.goto('/auth/login')`
   - Fill `#email` with `'wrong@example.com'`
   - Fill `#password` with `'wrongpassword123'`
   - Click `.btn-primary`
   - Wait for error: `await expect(page.locator('#error')).toBeVisible()`
   - Assert error text is not empty: `await expect(page.locator('#error')).not.toBeEmpty()`
   - Assert still on login page: `await expect(page).toHaveURL(/\/auth\/login/)`
   - Close context

3. **'register page renders form'** (AUTH-E2E-03):
   - Use a fresh browser context (no storage state)
   - `await page.goto('/auth/register')`
   - Assert form visible: `await expect(page.locator('#registerForm')).toBeVisible()`
   - Assert email field: `await expect(page.locator('#email')).toBeVisible()`
   - Assert password field: `await expect(page.locator('#password')).toBeVisible()`
   - Assert submit button: `await expect(page.locator('.btn-primary')).toBeVisible()`
   - **DO NOT submit the form** (would create a real user in production)
   - Close context

4. **'logout clears session and redirects to login'** (AUTH-E2E-04):
   - This test uses the `page` fixture (which has stored auth from the `pro` project)
   - Verify initially authenticated: `await page.goto('/dashboard')` then `await expect(page).toHaveURL(/\/dashboard/)`
   - Navigate to logout: `await page.goto('/auth/logout')`
   - Wait for redirect: `await page.waitForURL('**/auth/login**')`
   - Assert on login page: `await expect(page).toHaveURL(/\/auth\/login/)`
   - Verify session cleared by trying to access dashboard: `await page.goto('/dashboard')`
   - Assert redirected back to login (no session): `await expect(page).toHaveURL(/\/auth\/login/)`

**Important implementation notes:**
- For tests 1-3, do NOT use the default `page` fixture (it has stored auth). Instead, use `{ browser }` fixture to create a fresh context without storage state.
- Test 4 uses the default `page` fixture with stored auth from the `pro` project.
- The login form submits via JavaScript `fetch()` then `window.location.href = '/'`, which redirects to `/dashboard`. Use `page.waitForURL` to handle the JS-driven redirect.
- Use `test.use({ storageState: { cookies: [], origins: [] } })` inside a nested describe for unauthenticated tests instead of manually creating contexts. This is cleaner Playwright pattern.
  </action>
  <verify>
    <automated>npx playwright test e2e/tests/test_auth.spec.ts --config=e2e/playwright.config.ts --project=pro 2>&1 | Select-Object -Last 20</automated>
  </verify>
  <acceptance_criteria>
    - e2e/tests/test_auth.spec.ts contains `test.describe('Auth'`
    - e2e/tests/test_auth.spec.ts contains `test('valid login redirects to dashboard'`
    - e2e/tests/test_auth.spec.ts contains `test('invalid login shows error message'`
    - e2e/tests/test_auth.spec.ts contains `test('register page renders form'`
    - e2e/tests/test_auth.spec.ts contains `test('logout clears session and redirects to login'`
    - e2e/tests/test_auth.spec.ts contains `page.goto('/auth/login')`
    - e2e/tests/test_auth.spec.ts contains `page.locator('#loginForm')`
    - e2e/tests/test_auth.spec.ts contains `page.locator('#error')`
    - e2e/tests/test_auth.spec.ts contains `page.goto('/auth/register')`
    - e2e/tests/test_auth.spec.ts contains `page.locator('#registerForm')`
    - e2e/tests/test_auth.spec.ts contains `page.goto('/auth/logout')`
    - e2e/tests/test_auth.spec.ts contains `waitForURL('**/dashboard**')` or `toHaveURL(/\/dashboard/)`
    - e2e/tests/test_auth.spec.ts has NO form.submit() calls on register page
    - e2e/tests/test_auth.spec.ts reads E2E_PRO_EMAIL and E2E_PRO_PASSWORD from process.env
    - All 4 auth tests pass: `npx playwright test e2e/tests/test_auth.spec.ts --config=e2e/playwright.config.ts --project=pro` exits 0
  </acceptance_criteria>
  <done>4 auth E2E tests pass: valid login → dashboard, invalid login → error, register form renders, logout → clears session</done>
</task>

<task type="auto">
  <name>Task 2: Calendar view E2E tests</name>
  <files>e2e/tests/test_calendar.spec.ts</files>
  <read_first>
    - e2e/playwright.config.ts (project names, storageState for pro project)
    - app/templates/calendar.html (selectors: #month-grid, #day-events, #event-entry-open-btn, hx-get patterns)
    - app/views/calendar_routes.py (GET /calendar, GET /calendar/month, GET /calendar/day)
    - app/templates/partials/event_entry_modal.html (modal structure for event entry)
    - .planning/phases/37-core-app-e2e-tests/37-CONTEXT.md (calendar verification approach: read-only, no CRUD)
  </read_first>
  <action>
Create `e2e/tests/test_calendar.spec.ts` with these tests using `import { test, expect } from '@playwright/test'`:

**test.describe('Calendar', () => { ... })** containing:

1. **'month grid renders with day cells'** (CAL-E2E-01):
   - `await page.goto('/calendar')`
   - Wait for HTMX to load month grid content: `await page.locator('#month-grid').waitFor({ state: 'attached' })`
   - Wait for actual day cell content to appear inside the grid (HTMX loads it async):
     `await expect(page.locator('#month-grid td').first()).toBeVisible({ timeout: 15000 })`
   - Assert multiple day cells exist in the grid: `const cellCount = await page.locator('#month-grid td').count()`
   - Expect at least 28 cells (shortest month): `expect(cellCount).toBeGreaterThanOrEqual(28)`

2. **'month navigation changes displayed month'** (CAL-E2E-02):
   - `await page.goto('/calendar')`
   - Wait for month grid to load: `await expect(page.locator('#month-grid td').first()).toBeVisible({ timeout: 15000 })`
   - Find the "next month" navigation button. In the calendar template, navigation is typically a button/link that triggers an HTMX request to `/calendar/month` with updated year/month params. Look for a navigation control — check template for `prev-month` or `next-month` or arrow buttons.
   - Click the next month navigation element
   - Wait for HTMX to swap new content into `#month-grid`: use `await page.waitForResponse(resp => resp.url().includes('/calendar/month'))` to wait for the HTMX partial response
   - After swap, assert day cells are still present: `await expect(page.locator('#month-grid td').first()).toBeVisible()`

   **Selector discovery:** Read `app/templates/calendar.html` and `app/templates/partials/` to find the exact navigation button selectors. The month nav likely uses HTMX `hx-get` with updated month params. Look for elements with `hx-get="/calendar/month"` or data attributes for prev/next.

3. **'event entry button opens modal'** (CAL-E2E-03):
   - `await page.goto('/calendar')`
   - Wait for page to load: `await expect(page.locator('#month-grid td').first()).toBeVisible({ timeout: 15000 })`
   - Assert event entry button exists: `await expect(page.locator('#event-entry-open-btn')).toBeVisible()`
   - Click: `await page.locator('#event-entry-open-btn').click()`
   - Wait for modal to appear — the modal from `partials/event_entry_modal.html` should become visible. Check for a modal container becoming visible (e.g., a div with `role="dialog"` or a specific modal ID). Look in the template for the modal element ID.
   - Assert modal is visible: `await expect(page.locator('[role="dialog"]').first()).toBeVisible()` (or the specific modal element)
   - **DO NOT submit the form** — close modal or just verify it opened
   - If there's a close button, click it. Otherwise just assert the modal appeared.

**All tests use the default `page` fixture with stored auth (from the `pro` project).** No manual login needed — storage state handles auth.

**HTMX wait strategy:** The calendar uses `hx-trigger="load"` for `#month-grid`, which means content loads immediately after the page shell renders. Use `toBeVisible({ timeout: 15000 })` on content inside the container (not just the container div) to wait for HTMX completion.
  </action>
  <verify>
    <automated>npx playwright test e2e/tests/test_calendar.spec.ts --config=e2e/playwright.config.ts --project=pro 2>&1 | Select-Object -Last 20</automated>
  </verify>
  <acceptance_criteria>
    - e2e/tests/test_calendar.spec.ts contains `test.describe('Calendar'`
    - e2e/tests/test_calendar.spec.ts contains `test('month grid renders with day cells'`
    - e2e/tests/test_calendar.spec.ts contains `test('month navigation changes displayed month'`
    - e2e/tests/test_calendar.spec.ts contains `test('event entry button opens modal'`
    - e2e/tests/test_calendar.spec.ts contains `page.goto('/calendar')`
    - e2e/tests/test_calendar.spec.ts contains `#month-grid`
    - e2e/tests/test_calendar.spec.ts contains `#event-entry-open-btn`
    - e2e/tests/test_calendar.spec.ts uses timeout of at least 10000ms for HTMX content waits
    - e2e/tests/test_calendar.spec.ts has NO event creation/submission calls
    - All 3 calendar tests pass: `npx playwright test e2e/tests/test_calendar.spec.ts --config=e2e/playwright.config.ts --project=pro` exits 0
  </acceptance_criteria>
  <done>3 calendar E2E tests pass: month grid renders day cells, navigation changes month, event entry button opens modal</done>
</task>

</tasks>

<verification>
```bash
# Run all tests from this plan
npx playwright test e2e/tests/test_auth.spec.ts e2e/tests/test_calendar.spec.ts --config=e2e/playwright.config.ts --project=pro

# Verify test count
npx playwright test e2e/tests/test_auth.spec.ts e2e/tests/test_calendar.spec.ts --config=e2e/playwright.config.ts --project=pro --list
```
</verification>

<success_criteria>
- 4 auth tests pass (login, invalid login, register form, logout)
- 3 calendar tests pass (month grid, navigation, event modal)
- All tests are read-only — no data mutations in production
- Tests use stored auth from Phase 36 infrastructure (except auth tests that test login flow)
</success_criteria>

<output>
After completion, create `.planning/phases/37-core-app-e2e-tests/37-01-SUMMARY.md`
</output>
