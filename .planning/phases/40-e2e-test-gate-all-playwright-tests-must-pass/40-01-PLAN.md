---
phase: 40-e2e-test-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/templates/base.html]
autonomous: true
requirements: [GATE-01]

must_haves:
  truths:
    - "All 126 Playwright E2E tests pass with 0 failures"
    - "Pro test user has correct subscription plan in database"
    - "Calendar JS executes without errors — event listeners attached"
  artifacts:
    - path: "app/templates/base.html"
      provides: "formatReminderMinutes available before content block"
  key_links:
    - from: "app/templates/calendar.html"
      to: "app/templates/base.html"
      via: "formatReminderMinutes function call during script init"
      pattern: "formatReminderMinutes"
---

<objective>
Fix all 18 failing E2E tests (6 unique failures × 3 projects) to achieve 126/126 pass rate.

Purpose: Gate phase ensuring all Playwright tests pass before proceeding with brand/marketing phases.
Output: 0 test failures, deployed fix to production.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix calendar JS crash and pro user subscription</name>
  <files>app/templates/base.html</files>
  <action>
    1. Move formatReminderMinutes and pad2 function definitions from base.html (after content block) to before the content block, so calendar.html scripts can call them during init.
    2. Update pro test user subscription in Supabase from plan="free" to plan="pro".
  </action>
  <verify>
    <automated>npx playwright test --config=e2e/playwright.config.ts --reporter=line --retries=0</automated>
  </verify>
  <done>126/126 E2E tests pass, 0 failures</done>
</task>

</tasks>

<verification>
npx playwright test --config=e2e/playwright.config.ts --reporter=line --retries=0
All 126 tests pass across free, pro, and family-plus projects.
</verification>

<success_criteria>
- 126/126 Playwright E2E tests pass
- 0 failures across all 3 projects (free, pro, family-plus)
- Fix deployed to production
</success_criteria>

<output>
After completion, create `.planning/phases/40-e2e-test-gate-all-playwright-tests-must-pass/40-01-SUMMARY.md`
</output>
