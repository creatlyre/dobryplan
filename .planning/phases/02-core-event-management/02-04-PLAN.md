---
phase: 02-core-event-management
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - app/templates/calendar.html
  - app/templates/partials/event_form.html
  - tests/test_calendar_views.py
  - tests/test_events_integration.py
autonomous: true
requirements: [EVT-01, EVT-03, EVT-04, EVT-05, EVT-06, VIEW-01, VIEW-02, VIEW-03]
must_haves:
  truths:
    - User can create/edit/delete events from calendar page
    - UI updates reflect changes without full page reload
  artifacts:
    - path: app/templates/partials/event_form.html
      provides: form for create/edit event interactions
    - path: tests/test_events_integration.py
      provides: integration coverage for UI+API event flows
  key_links:
    - from: app/templates/calendar.html
      to: /api/events
      via: create/edit/delete form submissions
      pattern: /api/events
---

<objective>
Integrate CRUD actions into calendar UI and validate the full user flow.

Purpose: complete all remaining Phase 2 criteria and harden behavior with integration tests.
Output: fully interactive event management UI.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add event form interactions in UI</name>
  <files>app/templates/calendar.html, app/templates/partials/event_form.html</files>
  <action>Add responsive create/edit/delete controls wired to event API and refresh month/day panels after actions.</action>
  <verify><automated>python -c "from main import app; print('ok')"</automated></verify>
  <done>Users can perform CRUD from calendar page and see immediate updates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests for Phase 2 outcomes</name>
  <files>tests/test_calendar_views.py, tests/test_events_integration.py</files>
  <behavior>
    - Day list contains created event.
    - Month grid displays event title/time.
    - Month navigation changes rendered month.
  </behavior>
  <action>Add tests covering combined API + view behavior and verify response content for month/day rendering.</action>
  <verify><automated>python -m pytest tests/test_events_api.py tests/test_calendar_views.py tests/test_events_integration.py -q</automated></verify>
  <done>All phase-2 tests pass and UI requirements are covered.</done>
</task>
</tasks>

<verification>
Run combined event + calendar test suite.
</verification>

<success_criteria>
Phase 2 requirements are fully implemented and validated.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-event-management/02-04-SUMMARY.md`
</output>
