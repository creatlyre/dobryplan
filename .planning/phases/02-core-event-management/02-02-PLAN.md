---
phase: 02-core-event-management
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/events/routes.py
  - main.py
  - tests/test_events_api.py
autonomous: true
requirements: [EVT-01, EVT-03, EVT-04]
must_haves:
  truths:
    - User can create/edit/delete events via API
    - Deleted events no longer appear in event queries
  artifacts:
    - path: app/events/routes.py
      provides: FastAPI endpoints for event CRUD
    - path: tests/test_events_api.py
      provides: API coverage for CRUD behavior
  key_links:
    - from: main.py
      to: app/events/routes.py
      via: app.include_router(events_router)
      pattern: include_router\(events_router\)
---

<objective>
Expose event CRUD via authenticated APIs.

Purpose: satisfy backend behavior for EVT-01/03/04.
Output: /api/events endpoints with tests.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement events API router</name>
  <files>app/events/routes.py, main.py</files>
  <behavior>
    - POST /api/events creates event for current user's calendar.
    - PUT /api/events/{id} updates editable fields.
    - DELETE /api/events/{id} soft deletes event.
  </behavior>
  <action>Create authenticated endpoints using EventService and register router in main app.</action>
  <verify><automated>python -c "from app.events.routes import router; print('ok')"</automated></verify>
  <done>CRUD endpoints wired and importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add API tests</name>
  <files>tests/test_events_api.py, tests/conftest.py</files>
  <behavior>
    - Create returns 201 and payload includes id/title.
    - Update mutates title/time.
    - Delete hides event from subsequent lists.
  </behavior>
  <action>Add TestClient tests for authenticated CRUD operations with dependency overrides.</action>
  <verify><automated>python -m pytest tests/test_events_api.py -q</automated></verify>
  <done>API tests pass for create, update, delete behavior.</done>
</task>
</tasks>

<verification>
Run event API test suite.
</verification>

<success_criteria>
Authenticated event CRUD works end-to-end at API layer.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-event-management/02-02-SUMMARY.md`
</output>
