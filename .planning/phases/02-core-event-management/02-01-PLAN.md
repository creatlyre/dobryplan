---
phase: 02-core-event-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/events/__init__.py
  - app/events/schemas.py
  - app/events/repository.py
  - app/events/service.py
autonomous: true
requirements: [EVT-01, EVT-03, EVT-04]
must_haves:
  truths:
    - User can create a one-time event with title/date/time
    - User can edit and delete existing event
  artifacts:
    - path: app/events/service.py
      provides: Event CRUD business logic scoped by calendar_id
    - path: app/events/repository.py
      provides: SQLAlchemy persistence for event CRUD
  key_links:
    - from: app/events/service.py
      to: app/events/repository.py
      via: EventService uses EventRepository for persistence
      pattern: EventRepository
---

<objective>
Create the event-domain backend foundation with contracts, repository, and service APIs for event CRUD.

Purpose: establish clean interfaces for API and UI layers to consume.
Output: event schemas + repository + service abstractions.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build event contracts and repository</name>
  <files>app/events/__init__.py, app/events/schemas.py, app/events/repository.py</files>
  <behavior>
    - Creating event persists row with calendar ownership.
    - Updating event mutates only target event in user calendar.
    - Soft delete marks is_deleted=true.
  </behavior>
  <action>Create Pydantic schemas and SQLAlchemy repository methods create/get/update/delete/list_for_day/list_for_month scoped by calendar_id and ignoring soft-deleted rows.</action>
  <verify><automated>python -c "from app.events.repository import EventRepository; from app.events.schemas import EventCreate; print('ok')"</automated></verify>
  <done>Contracts and repository compile and enforce calendar scoping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build event service logic</name>
  <files>app/events/service.py</files>
  <behavior>
    - Service validates time range start_at < end_at.
    - Service returns only event rows visible to current household calendar.
  </behavior>
  <action>Implement EventService methods for create/update/delete/list day/list month with validation and optimistic edit metadata updates.</action>
  <verify><automated>python -c "from app.events.service import EventService; print('ok')"</automated></verify>
  <done>Service is importable and guards invalid time ranges.</done>
</task>
</tasks>

<verification>
Core event backend modules import successfully.
</verification>

<success_criteria>
Event backend foundation exists and is ready for API wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-event-management/02-01-SUMMARY.md`
</output>
