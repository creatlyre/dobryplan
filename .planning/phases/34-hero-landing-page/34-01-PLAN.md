---
phase: 34-hero-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/landing.html
  - app/locales/pl.json
  - app/locales/en.json
  - tests/test_go_to_market.py
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
must_haves:
  truths:
    - "Unauthenticated visitor at / sees a social proof section with trust indicators"
    - "Primary hero CTA points to /auth/register (not /auth/login)"
    - "Page has og:title, og:description, og:image, og:url meta tags"
    - "Page has twitter:card meta tag"
    - "All new text available in both Polish and English"
    - "Tests validate social proof section, OG tags, and dual CTAs"
  artifacts:
    - path: "app/templates/landing.html"
      provides: "Enhanced landing page with social proof, OG meta, dual CTAs"
      contains: "og:title"
    - path: "app/locales/pl.json"
      provides: "Polish translations for social proof section"
      contains: "landing.trust_"
    - path: "app/locales/en.json"
      provides: "English translations for social proof section"
      contains: "landing.trust_"
    - path: "tests/test_go_to_market.py"
      provides: "Test coverage for social proof, OG tags, dual CTAs"
      contains: "test_landing_page_has_og_tags"
  key_links:
    - from: "app/templates/landing.html"
      to: "app/locales/pl.json"
      via: "t() i18n function calls"
      pattern: "t\\('landing\\.trust"
    - from: "app/templates/landing.html"
      to: "/auth/register"
      via: "Primary CTA href"
      pattern: "href=.*/auth/register"
    - from: "app/templates/landing.html"
      to: "/auth/login"
      via: "Secondary CTA and nav sign-in"
      pattern: "href=.*/auth/login"
---

<objective>
Enhance the existing landing page at `/` with social proof trust indicators, Open Graph / Twitter Card SEO meta tags, improved hero with dual CTAs (register primary + sign-in secondary), and full PL/EN i18n for all new content. Add tests for all new sections.

Purpose: Increase conversion from first-time visitors by adding trust signals and SEO shareability. The Phase 33 landing page is functional but lacks social proof and social sharing metadata.

Output: Enhanced `landing.html`, updated locale files, new test coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-hero-landing-page/34-CONTEXT.md

@app/templates/landing.html
@app/locales/pl.json
@app/locales/en.json
@main.py
@tests/test_go_to_market.py

<interfaces>
<!-- Key routes and patterns the executor needs -->

From main.py (root route, lines 181-188):
```python
@app.get("/", response_class=HTMLResponse)
async def root(request: Request, user=Depends(get_current_user_optional)):
    if user:
        return RedirectResponse(url="/dashboard", status_code=302)
    context = inject_template_i18n(request, {"request": request})
    response = templates.TemplateResponse(request=request, name="landing.html", context=context)
    set_locale_cookie_if_param(response, request)
    return response
```

From app/i18n.py:
```python
# Templates use t('key') function for translations
# Locale resolved from cookie/param via resolve_locale(request)
```

Existing landing.html structure (208 lines):
- Lines 1-27: Head (meta, fonts, inline CSS with animations)
- Lines 29-42: Nav bar (logo, pricing link, sign-in button)
- Lines 44-92: Hero section (title, subtitle, CTAs, CSS calendar mockup)
- Lines 94-115: Features section (6 feature cards in grid)
- Lines 117-143: How It Works (3 steps)
- Lines 145-192: Pricing preview (Free/Pro/Family cards)
- Lines 194-208: Footer (legal links, language switcher)

Existing i18n keys (landing.*):
- landing.meta_desc, landing.nav_pricing, landing.nav_sign_in
- landing.hero_title ("Jeden kalendarz dla dwojga"), landing.hero_subtitle, landing.hero_cta, landing.hero_secondary
- landing.features_title, landing.feat_*_title/desc (6 features)
- landing.how_title, landing.step*_title/desc (3 steps)
- landing.pricing_title, landing.pricing_subtitle, landing.self_hosted_link
- landing.footer_made_with

Existing tests (test_go_to_market.py):
```python
def test_unauthenticated_root_shows_landing(self, test_client): ...
def test_landing_page_has_cta(self, test_client): ...
def test_landing_page_has_features(self, test_client): ...
def test_authenticated_root_redirects_to_dashboard(self, authenticated_client): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add social proof section, OG meta tags, and dual CTAs to landing page</name>
  <files>app/templates/landing.html, app/locales/pl.json, app/locales/en.json</files>
  <read_first>
    - app/templates/landing.html (full file — current structure)
    - app/locales/pl.json (landing.* keys around lines 554-584)
    - app/locales/en.json (matching landing.* keys)
  </read_first>
  <behavior>
    - Test: GET / response contains `og:title` meta tag
    - Test: GET / response contains `og:description` meta tag
    - Test: GET / response contains `og:image` meta tag
    - Test: GET / response contains `og:url` meta tag
    - Test: GET / response contains `twitter:card` meta tag
    - Test: GET / response contains `/auth/register` (primary CTA href)
    - Test: GET / response contains trust indicator section (search for `landing.trust_` or trust-related class)
    - Test: pl.json has key `landing.trust_open_source`
    - Test: en.json has key `landing.trust_open_source`
  </behavior>
  <action>
**1. Add OG and Twitter Card meta tags to `<head>` section of landing.html.**

Insert after the existing `<meta name="description">` tag (line 6):

```html
<!-- Open Graph -->
<meta property="og:type" content="website">
<meta property="og:title" content="{{ t('landing.og_title') }}">
<meta property="og:description" content="{{ t('landing.meta_desc') }}">
<meta property="og:image" content="/static/icons/og-image.png">
<meta property="og:url" content="{{ request.url }}">
<meta property="og:locale" content="{{ locale|default('pl') }}">
<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="{{ t('landing.og_title') }}">
<meta name="twitter:description" content="{{ t('landing.meta_desc') }}">
<meta name="twitter:image" content="/static/icons/og-image.png">
```

**2. Change primary hero CTA from `/auth/login` to `/auth/register`.**

In the hero section, the primary CTA anchor (currently `href="/auth/login"` with class containing `bg-indigo-600`):
- Change `href="/auth/login"` to `href="/auth/register"`
- Keep the text `{{ t('landing.hero_cta') }}`

The secondary CTA (`#pricing` link) stays as-is.

**3. Add Social Proof / Trust Indicators section.**

Insert a new section between the Hero section and the Features section. Use 3 trust badges in a horizontal row:

```html
{# Trust indicators #}
<section class="py-12 border-b border-white/5">
    <div class="mx-auto max-w-4xl px-4">
        <p class="text-center text-sm text-gray-400 mb-6">{{ t('landing.trust_headline') }}</p>
        <div class="flex flex-col sm:flex-row items-center justify-center gap-6 sm:gap-10">
            <div class="flex items-center gap-2 text-sm text-gray-300">
                <svg class="w-5 h-5 text-emerald-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
                {{ t('landing.trust_open_source') }}
            </div>
            <div class="flex items-center gap-2 text-sm text-gray-300">
                <svg class="w-5 h-5 text-blue-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22c5.523 0 10-4.477 10-10S17.523 2 12 2 2 6.477 2 12s4.477 10 10 10z"/><path d="m9 12 2 2 4-4"/></svg>
                {{ t('landing.trust_gdpr') }}
            </div>
            <div class="flex items-center gap-2 text-sm text-gray-300">
                <svg class="w-5 h-5 text-indigo-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="2" width="14" height="20" rx="2" ry="2"/><line x1="12" y1="18" x2="12.01" y2="18"/></svg>
                {{ t('landing.trust_pwa') }}
            </div>
        </div>
    </div>
</section>
```

**4. Add new i18n keys to both locale files.**

Add to `pl.json` (after `landing.footer_made_with` key):
```json
"landing.og_title": "Synco — wspólny kalendarz dla par",
"landing.trust_headline": "Dołącz do rosnącej społeczności",
"landing.trust_open_source": "Open source (AGPL-3.0)",
"landing.trust_gdpr": "Zgodny z RODO",
"landing.trust_pwa": "Zainstaluj na telefonie"
```

Add to `en.json` (matching position):
```json
"landing.og_title": "Synco — shared calendar for couples",
"landing.trust_headline": "Join our growing community",
"landing.trust_open_source": "Open source (AGPL-3.0)",
"landing.trust_gdpr": "GDPR compliant",
"landing.trust_pwa": "Install on your phone"
```
  </action>
  <verify>
    <automated>python -m pytest tests/test_go_to_market.py -v -x --tb=short 2>&1 | Select-Object -Last 30</automated>
  </verify>
  <acceptance_criteria>
    - landing.html contains `property="og:title"`
    - landing.html contains `property="og:description"`
    - landing.html contains `property="og:image"`
    - landing.html contains `property="og:url"`
    - landing.html contains `name="twitter:card"`
    - landing.html contains `href="/auth/register"` (primary CTA)
    - landing.html contains `t('landing.trust_open_source')`
    - landing.html contains `t('landing.trust_gdpr')`
    - landing.html contains `t('landing.trust_pwa')`
    - pl.json contains `"landing.og_title"`
    - pl.json contains `"landing.trust_headline"`
    - pl.json contains `"landing.trust_open_source"`
    - pl.json contains `"landing.trust_gdpr"`
    - pl.json contains `"landing.trust_pwa"`
    - en.json contains `"landing.og_title"`
    - en.json contains `"landing.trust_headline"`
    - en.json contains `"landing.trust_open_source"`
    - en.json contains `"landing.trust_gdpr"`
    - en.json contains `"landing.trust_pwa"`
    - tests/test_go_to_market.py contains `def test_landing_page_has_og_tags`
    - tests/test_go_to_market.py contains `def test_landing_page_has_social_proof`
    - tests/test_go_to_market.py contains `def test_landing_page_has_register_cta`
    - `python -m pytest tests/test_go_to_market.py -v` exits 0
  </acceptance_criteria>
  <done>
    - Landing page has OG and Twitter Card meta tags in head
    - Primary hero CTA links to /auth/register
    - Trust indicators section visible between hero and features
    - All new text in both PL and EN locale files
    - All tests pass including new ones for OG tags, social proof, and register CTA
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_go_to_market.py -v` — all tests pass
2. `python i18n_check.py` — no missing i18n keys
3. `curl -s http://localhost:8000/ | grep -c "og:title"` — returns 1+
4. `curl -s http://localhost:8000/ | grep -c "auth/register"` — returns 1+
5. `curl -s http://localhost:8000/ | grep -c "trust"` — returns 1+ (trust section present)
</verification>

<success_criteria>
- Unauthenticated GET / returns landing page with OG meta tags, social proof section, and register CTA
- All 31+ existing landing.* i18n keys preserved, 5 new keys added to both PL and EN
- All existing tests still pass, 3+ new tests added and passing
- No regressions in authenticated redirect to /dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/34-hero-landing-page/34-01-SUMMARY.md`
</output>
