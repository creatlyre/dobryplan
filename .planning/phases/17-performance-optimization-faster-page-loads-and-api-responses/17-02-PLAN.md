---
phase: 17-performance-optimization
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - tests/test_overview.py
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03]

must_haves:
  truths:
    - "All existing tests pass after performance changes"
    - "Connection pooling verified by test"
    - "Pages render correctly with prebuilt CSS"
  artifacts:
    - path: "tests/test_overview.py"
      provides: "Regression + performance verification tests"
  key_links:
    - from: "tests/test_overview.py"
      to: "app/database/supabase_store.py"
      via: "SupabaseStore import"
      pattern: "SupabaseStore"
---

<objective>
Run full test suite to verify no regressions from performance changes, and human-verify visual correctness.

Purpose: Ensure Tailwind CSS build and connection pooling don't break anything.
Output: All tests passing + human-verified UI.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-performance-optimization-faster-page-loads-and-api-responses/17-CONTEXT.md
@.planning/phases/17-performance-optimization-faster-page-loads-and-api-responses/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression test suite</name>
  <files>tests/</files>
  <action>
Run the complete test suite to verify all 222+ tests still pass after:
1. Tailwind CDN removal + prebuilt CSS
2. SupabaseStore connection pooling changes
3. StaticFiles mount in main.py

If any tests fail, diagnose and fix. Common issues:
- Template rendering tests may need adjustment if base.html structure changed significantly
- Import paths may need updating if database module structure changed
  </action>
  <verify>
    <automated>python -m pytest --tb=short 2>&1 | Select-Object -Last 10</automated>
  </verify>
  <done>All 222+ tests pass with zero failures after performance optimizations</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all pages</name>
  <files>app/templates/base.html</files>
  <action>Verify all pages render correctly with the prebuilt Tailwind CSS (no broken styles).</action>
  <what-built>
    - Tailwind CDN replaced with prebuilt CSS file
    - SupabaseStore now uses connection pooling
    - Static files served via FastAPI StaticFiles
  </what-built>
  <how-to-verify>
    1. Start dev server: `uvicorn main:app --reload`
    2. Open http://localhost:8000 — calendar page glass morphism styling intact
    3. Open http://localhost:8000/budget/overview — overview table renders correctly
    4. Open http://localhost:8000/budget/settings — settings form renders correctly
    5. Open http://localhost:8000/budget/income — income grid renders correctly
    6. Open http://localhost:8000/budget/expenses — expense tables render correctly
    7. Check browser DevTools Network tab — no cdn.tailwindcss.com request
    8. Verify /static/css/style.css loads successfully
    9. Check responsive behavior (resize window) — layouts adapt correctly
    10. Test light/dark mode toggle if available
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>Human confirms UI looks correct</verify>
  <done>All pages render correctly with prebuilt CSS, no CDN dependency, responsive layouts intact</done>
</task>

</tasks>

<verification>
- Full test suite passes
- Human confirms visual correctness across all pages
</verification>

<success_criteria>
222+ tests passing. All pages render with correct styling from prebuilt CSS. No Tailwind CDN dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/17-performance-optimization-faster-page-loads-and-api-responses/17-02-SUMMARY.md`
</output>
