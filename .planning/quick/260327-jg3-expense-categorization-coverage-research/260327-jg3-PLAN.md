---
phase: 260327-jg3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/budget/category_keywords.json
  - scripts/validate_categorization.py
  - scripts/categorization_results.json
autonomous: true
requirements: [CATCOV-01]

must_haves:
  truths:
    - "category_keywords.json covers all 20 preset categories with comprehensive PL+EN keywords"
    - "Known collision issues (szampon→Pets, tea→theater) are resolved via keyword additions"
    - "1,000 generated test expenses (10×100) achieve ≥93% categorization hit rate"
    - "Validation results are saved as a JSON artifact with per-list and aggregate stats"
  artifacts:
    - path: "app/budget/category_keywords.json"
      provides: "Updated keyword map with ~120 new keywords across thin categories"
      contains: "loan|installment|repayment|debt"
    - path: "scripts/validate_categorization.py"
      provides: "Test data generator + categorization validator"
      min_lines: 80
    - path: "scripts/categorization_results.json"
      provides: "1,000-item validation results with hit rates"
      contains: "aggregate_hit_rate"
  key_links:
    - from: "scripts/validate_categorization.py"
      to: "app/budget/expense_service.py"
      via: "Replicates _normalize() and _detect_category() matching logic"
      pattern: "_normalize|norm_kw in w"
    - from: "app/budget/category_keywords.json"
      to: "app/budget/expense_service.py"
      via: "_load_keywords() at module import"
      pattern: "category_keywords\\.json"
---

<objective>
Validate and improve expense categorization coverage for PL+EN keywords across all 20 preset categories.

Purpose: Ensure the keyword-based auto-categorization system reliably classifies real-world household expenses, catching gaps before users encounter uncategorized items.
Output: Updated category_keywords.json with ~120 new keywords, a validation script, and JSON results proving ≥93% hit rate across 1,000 test expenses.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/budget/category_keywords.json
@app/budget/expense_service.py
@app/budget/expense_repository.py (lines 116-137 — _PRESET_CATEGORIES)
@.planning/quick/260327-jg3-expense-categorization-coverage-research/260327-jg3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ~120 keywords to category_keywords.json and fix collisions</name>
  <files>app/budget/category_keywords.json</files>
  <action>
Add missing keywords to category_keywords.json based on RESEARCH.md gap analysis. Keyword additions only — NO algorithm changes.

**CRITICAL (Loan — currently 3 keywords, add ~15):**
- PL: `rata`, `raty`, `splata`, `dlug`, `chwilowka`, `windykacja`, `komornik`, `refinansowanie`
- EN: `loan`, `installment`, `repayment`, `debt`, `financing`, `credit payment`, `monthly payment`, `interest`

**CRITICAL (Travel — add ~15):**
- Airlines: `ryanair`, `wizzair`, `wizz air`, `easyjet`, `lot`
- Generic PL: `wycieczka`, `kemping`, `camping`, `namiot`, `turystyka`, `wczasy`, `agroturystyka`, `kolonie`, `oboz`
- EN: `cruise`, `excursion`, `resort`, `tour`, `souvenir`

**CRITICAL (Rent — add ~10):**
- PL: `fundusz remontowy`, `podatek od nieruchomosci`, `oplata za mieszkanie`, `zarzadca`, `wspolnota mieszkaniowa`
- EN: `property tax`, `HOA`, `maintenance fee`, `homeowners`

**MODERATE categories (add 5-15 each):**
- Personal Care: `manicure`, `pedicure`, `paznokcie`, `depilacja`, `spa`, `masaz`, `massage`, `salon`, `skincare`
- Health: pharmacy chains `gemini`, `doz`, `dbam o zdrowie`; terms `stomatologia`, `implant`, `orteza`; drugs `apap`, `ibuprom`, `paracetamol`, `nurofen`
- Children: `dziecko`, `butelka`, `smoczek`, `fotelik`, `mleko modyfikowane`, `formula`, `baby`, `child`, `kids`, `toddler`; stores `coccodrillo`, `51015`
- Education: `szkola jezykowa`, `kurs online`, `korepetycje`, `e-learning`, `online course`
- Clothing: stores `deichmann`, `ccc`, `halfprice`, `tkmaxx`, `tk maxx`, `zalando`, `modivo`, `aboutyou`; brands `nike`, `adidas`, `new balance`, `puma`
- Electronics: `smartfon`, `smartphone`, `telewizor`, `tv`, `apple`, `samsung`, `xiaomi`
- Savings & Finance: `inwestycja`, `investment`, `fundusz`, `lokata`, `obligacje`, `bonds`, `akcje`, `stocks`, `prowizja`, `broker`, `makler`
- Groceries: `piekarnia`, `bakery`, `masarnia`, `butcher`, `delikatesy`, `warzywniak`
- Transport: `e-toll`, `mandat`, `laweta`, `holowanie`, `bilet miesieczny`; apps `jakdojade`, `koleo`
- Pets: brands `royal canin`, `purina`, `whiskas`, `pedigree`; `akwarium`, `terrarium`
- Events: `zaproszenie`, `invitation`, `dekoracje`, `balony`, `balloon`
- Shopping: `inpost`, `paczkomat`, `kurier`, `dhl`, `dpd`, `paczka`, `przesylka`, `zamowienie`

**Collision fixes (keyword additions only, no reorder):**
- Add standalone `"szampon"` to Personal Care — it already exists there, so verify. The fix is: remove `"szampon dla psow"` and `"szampon dla ps"` from Pets (these cause reverse-substring theft). Keep pet-specific `"karma psy"`, `"karma kot"` etc.
- Add `"theater"` and `"theatre"` explicitly to Entertainment (already there — verify they match before `"tea"` in Groceries steals them). Actually the issue is word-level: `"tea"` is a substring of the word `"theater"`. Since Entertainment already has `"theater"` as a keyword, and Groceries checks first... The simplest fix: this only affects EN; in PL `"teatr"` is fine. Accept as known edge case — document in results.

Preserve JSON formatting: 2-space indent, trailing newline, arrays with comma-separated strings.
  </action>
  <verify>
    <automated>cd C:\Users\Wojciech\Documents\Python\CalendarPlanner; python -c "import json; d=json.load(open('app/budget/category_keywords.json','utf-8')); cats={k for k in d if not k.startswith('_')}; print(f'Categories: {len(cats)}'); print(f'Loan keywords: {len(d.get(\"Loan\",[]))}'); print(f'Travel keywords: {len(d.get(\"Travel\",[]))}'); assert len(d.get('Loan',[])) >= 15, 'Loan too thin'; assert len(d.get('Travel',[])) >= 30, 'Travel too thin'; print('OK')"</automated>
  </verify>
  <done>category_keywords.json has ~120 new keywords across all thin categories. Loan ≥15, Travel ≥30, Rent ≥30 keywords. No JSON syntax errors. Pets no longer has "szampon" reverse-match keywords.</done>
</task>

<task type="auto">
  <name>Task 2: Generate 10×100 test data, validate categorization at ≥93%, save results</name>
  <files>scripts/validate_categorization.py, scripts/categorization_results.json</files>
  <action>
Create `scripts/validate_categorization.py` that:

1. **Replicates the exact matching logic** from `expense_service.py`:
   - `_normalize()`: lowercase, `ł→l`, NFKD strip combining chars
   - Load `category_keywords.json`, strip `_meta`, pre-normalize keywords
   - Match: for each word in expense name, check `norm_kw in w` or (`len(w)>=3` and `w in norm_kw`), first-match-wins across categories in JSON key order

2. **Contains curated test pools per category** (hardcoded dicts):
   - Each of the 19 non-Other categories gets a pool of 15-30 realistic expense names (PL ~60%, EN ~30%, brand-only ~10%)
   - One "uncategorizable" pool of ~20 genuinely ambiguous items (`napiwek`, `darowizna`, `przelew`, `mandat`, `tandeta`, person names, etc.)
   - Distribution per 100-item list follows RESEARCH.md table: Groceries=20, Home=10, Utilities=8, Transport=8, Health=6, Entertainment=6, Children=5, Clothing=5, Personal Care=4, Education=3, Savings=3, Events=3, Pets=2, Garden=2, Electronics=2, Rent=2, Loan=1, Travel=1, Shopping=2, Uncategorizable=7

3. **Generates 10 lists of 100 items** by random.sample from each category pool (with replacement across lists if pool < needed), shuffled within each list. Use `random.seed(42)` for reproducibility.

4. **Runs categorization** on each item, comparing detected category vs expected category.

5. **Outputs JSON** to `scripts/categorization_results.json`:
```json
{
  "generated_at": "2026-03-27T...",
  "total_items": 1000,
  "categorized": N,
  "uncategorized": N,
  "correct": N,
  "wrong_category": N,
  "aggregate_hit_rate": 0.XX,
  "per_list_rates": [0.XX, ...],
  "per_category_stats": {
    "Groceries": {"total": N, "hit": N, "miss": N},
    ...
  },
  "misses": [
    {"list": 1, "item": "...", "expected": "...", "got": "..."},
    ...
  ]
}
```

6. **Prints summary** to stdout: aggregate rate, per-category rates, list of misses.

7. **If aggregate rate < 93%:** Print gap analysis showing which categories need more keywords. The script itself does NOT modify keywords — just reports gaps for manual iteration.

Run the script. If rate < 93%, go back to Task 1 and add more keywords to close gaps, then re-run. Iterate until ≥93% achieved.
  </action>
  <verify>
    <automated>cd C:\Users\Wojciech\Documents\Python\CalendarPlanner; python scripts/validate_categorization.py; python -c "import json; r=json.load(open('scripts/categorization_results.json')); print(f'Hit rate: {r[\"aggregate_hit_rate\"]:.1%}'); assert r['aggregate_hit_rate'] >= 0.93, f'Hit rate {r[\"aggregate_hit_rate\"]:.1%} < 93%'"</automated>
  </verify>
  <done>10×100 test items generated, categorization validated at ≥93% aggregate hit rate, results saved to scripts/categorization_results.json with per-list rates, per-category stats, and miss details.</done>
</task>

</tasks>

<verification>
1. `python -c "import json; json.load(open('app/budget/category_keywords.json','utf-8'))"` — JSON valid
2. `python scripts/validate_categorization.py` — runs without errors, prints summary
3. Aggregate hit rate in categorization_results.json ≥ 0.93
4. All 20 categories present in per_category_stats
5. Existing tests still pass: `python -m pytest tests/test_expenses.py -x -q`
</verification>

<success_criteria>
- category_keywords.json updated with ~120 new PL+EN keywords across thin categories
- Known collision ("szampon" Pets steal) resolved
- 1,000 test expense names generated across realistic distribution
- Aggregate categorization hit rate ≥93%
- Results saved as JSON artifact at scripts/categorization_results.json
</success_criteria>

<output>
After completion, create `.planning/quick/260327-jg3-expense-categorization-coverage-research/260327-jg3-SUMMARY.md`
</output>
